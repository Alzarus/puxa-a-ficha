version: "3.8"

env_file:
  - .env.production

services:
  db:
    image: postgres:13
    environment:
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_DATABASE}
    volumes:
      - db_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  api:
    build: ./api
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_DATABASE: ${DB_DATABASE}
    depends_on:
      - db
    ports:
      - "3000:3000"
    volumes:
      - ./api:/app

  crawlers:
    build:
      context: ./crawlers
      dockerfile: Dockerfile
    depends_on:
      - api
    volumes:
      - ./crawlers:/app/crawlers

  json-processor:
    build: ./json-processor
    depends_on:
      - crawlers
      - api
    volumes:
      - ./crawlers:/app/crawlers
    command: npm run process-json

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - api

volumes:
  db_data:
